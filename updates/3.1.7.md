# What has been updated?

## Changelog - (Added, Fixed, Removed):

1. Added a new system - billing system which has been made only for developers who can create bills for the players to pay through a telegram.
2. Added oxtarget support for opening the mailbox office.
3. Improved many code and tasks for better performance.

## Modified Files:

- html (REPLACE ALL FILES)
- client/** (ALL CLIENT FILES)
- server/** (ALL SERVER FILES)
- locales.lua (check notes)
- fxmanifest.lua (IMPORTANT)

## Notes

1. Add in the database (tp_mailbox) the new columns.

```sql
ALTER TABLE tp_mailbox
ADD COLUMN IF NOT EXISTS bill INT(1) DEFAULT 0,
ADD COLUMN IF NOT EXISTS bill_type VARCHAR(50) CHARACTER SET utf16 COLLATE utf16_unicode_ci DEFAULT NULL,
ADD COLUMN IF NOT EXISTS bill_cost INT(11) DEFAULT 0;
```

Alternative (If doesnt work)

```sql
SELECT COLUMN_NAME 
FROM INFORMATION_SCHEMA.COLUMNS 
WHERE TABLE_NAME = 'tp_mailbox' 
AND COLUMN_NAME IN ('bill', 'bill_type', 'bill_cost');
```

3. Add the new files that have been added to the script:

- client/tp-client_escrow_ignore.lua
- client/tp-client_api.lua
- server/tp-client_escrow_ignore.lua
- server/tp-server_api.lua

3. Add the new configuration option in every mailbox office:

```lua
-- Set to true if you are using target eye for opening the mailbox office. 
-- (!) Required an NPC Enabled, otherwise must be set to false.
TargetEye = { Enabled = true, Script = 'ox_target', Display = 'Open Mailbox Office'}, -- version 3.1.7
```

<img width="894" height="937" alt="image" src="https://github.com/user-attachments/assets/5db8e1dd-016c-41f5-838b-0f0f0aac8332" />

4. Add the new locale on `locales.lua` file:

```lua
['TELEGRAM_BILL_NOT_ENOUGH_MONEY'] = "You don't have enough money to pay for this bill.", -- version 3.1.7
```

<img width="880" height="783" alt="image" src="https://github.com/user-attachments/assets/1040e61d-570d-4ca4-9f98-493bd94fe24f" />

## Screenshots

<img width="1261" height="1222" alt="image" src="https://github.com/user-attachments/assets/7143006f-f64e-45e1-b11b-31eb66b95a5a" />
<img width="1804" height="1227" alt="image" src="https://github.com/user-attachments/assets/73171816-fa51-48bd-b633-c329c736f76c" />

## How to update?

- You can easily update the modified files by redownloading from the **Cfx.re Portal** and copy-paste on the mentioned directory file paths.
- If new options have been implemented to the configuration file or edited, **DO NOT** copy-paste the new configuration file, otherwise you will get the latest configuration but you will lose all the changes you have made. The proper way updating a configuration file is by finding the new configuration options or the modified ones and do it **manually**.

*Cfx.re Portal **DOES NOT** force-update your configuration file or any files, if it did, you would always lose all the configuration changes you made or would totally break the script, all files are updated or edited manually.*
